%article.lesson--organizations
  .thumb_image
    [TMB]
  .information
    %small
      =org.name
    %br
    %small
      ="#{user.getInvolvements.first.titleize} • #{user.primaryOrg.name.titleize}"