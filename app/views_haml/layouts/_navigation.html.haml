%nav{"role" => 'navigation'}
  .wrapped
    .navbar-header
      %h1{"class"=>"brand"}
        = link_to 'SCOPESdf', root_path
      = link_to '<i class="icon-Menu"></i><span>Menu</span>'.html_safe, '#', id: 'toggle-nav'
    .collapse.navbar-collapse
      %ul.nav.navbar-nav
        = render 'layouts/navigation_links'
        = render 'layouts/nav_links_for_auth'
