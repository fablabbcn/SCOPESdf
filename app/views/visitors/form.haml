-#= form_tag("/file_upload", method: "post", multipart: true) do
-#  = file_field_tag 'avatar'
-#  = submit_tag("Submit")

-#= form_tag(lesson_file_path(atr: "assessment_criteria", id: 2), method: "post", multipart: true) do
-#  = file_field_tag 'files[]', multiple: true
-#  = submit_tag("Submit")

