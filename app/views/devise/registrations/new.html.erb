<%= render partial: 'sub_header' %>

<%= render partial: 'new_sub_nav' %>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), multiple: true, html: { class: "Form" }) do |f| %>
  <%= devise_error_messages! %>
  <%= render "#{session[:registration_step]}_form", f: f %>
<% end %>
